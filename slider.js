let currentSlide = 0;

function startSlider() {
    let slider = document.getElementById("slider");
    let sliderImagesCount = slider.querySelectorAll("img");
    
}
setInterval(() => {
    startSlider();    
}, 5000);

